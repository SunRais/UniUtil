<div class="container">
	<div class="row">
		<div class="col-md-8">
			<div class="post-info">
				<h1 class="post-titolo"><%= @user.name %> <%= @user.surname %> </h1>
				<h2 class="post-titolo">Email: <%= @user.email %></h2>
				<h3 class="post-titolo">Numero di matricola: <%= @user.badge_number %></h3>
				<h3 class="post-titolo">Indirizzo: <%= @user.address %></h3>
				<h3 class="post-titolo">Città: <%= @user.city %></h3>
				<h3 class="post-titolo">Recapito telefonico: <%= @user.telephone_number %></h3>
			</div>
		</div>
	</div>
	<div class="links btn-group">
		<%= link_to "Indietro", root_path, class: "btn btn-custom" %>
	</div>
</div>